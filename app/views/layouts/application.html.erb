<!DOCTYPE html>
<html>
  <head>
    <title>Lab11</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
      <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
        <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"/></svg>
        <span class="fs-4"><strong>Лаб11</strong></span>
        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
        <li><strong><%= link_to "Главная страница", home_path, class: 'nav-link px-2 text-black' %></strong></li>
        <li><strong><%= link_to "Посмотреть все данные в базе данных", xml_path, class: 'nav-link px-2 text-black' %></strong></li>
        </ul>
      </a>
    </header>
  <body>
  <div class = "container">
    <% flash.each do |type, msg| %>
      <div class='alert alert-info'>
        <strong id='msg'><%= msg %></strong>
      </div>
    <% end %>
    <%= yield %>
    </div>
  </body>
</html>
